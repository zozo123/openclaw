name: Databricks ClawHub preview

on:
  push:
    branches:
      - databricks-provider-external

permissions:
  contents: read
  id-token: write

jobs:
  preview:
    uses: openclaw/clawhub/.github/workflows/package-publish.yml@v0.23.3
    with:
      dry_run: true
      source_path: extensions/databricks
